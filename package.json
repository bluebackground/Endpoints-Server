{
  "scripts": {
    "test": "mocha server/tests/endpoint-lists.test.js",
    "test-lists": "export NODE_ENV=test || set \"NODE_ENV=test\" && mocha --exit server/tests/endpoint-lists.test.js",
    "test-lists2": "export NODE_ENV=test || set \"NODE_ENV=test\" && mocha --exit server/tests/endpoint-lists2.test.js",
    "test-watch": "nodemon --exec 'yarn test'",
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "express": "^4.16.2",
    "mocha": "^4.0.1",
    "mongoose": "^4.13.0"
  }
}
